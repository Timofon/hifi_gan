_target_: src.logger.WandBWriter
project_name: "hi_fi_gan"
entity: null
run_name: "main"
mode: "online"
loss_names: ["generator_loss", "discriminator_loss"] # which losses to log (useful for multi-loss tasks, s.a. GANs)
log_checkpoints: True # set to True if you want to log save_dir with checkpoints to W&B
id_length: 8 # length of the run_id
